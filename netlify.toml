[build]
  command = "npm run build"
  publish = "dist"

# Cette section est utile si vous utilisez React Router pour gérer la navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
